ROSTemplateFormatVersion: '2015-09-01'
Description: Violation - Security group ingress with all ports open

Resources:
  # Violation: Security group with all ports open
  SecurityGroup:
    Type: ALIYUN::ECS::SecurityGroup
    Properties:
      SecurityGroupName: test-sg
      SecurityGroupIngress:
        - IpProtocol: tcp
          PortRange: -1/-1
          SourceCidrIp: 10.0.0.0/8

