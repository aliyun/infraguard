ROSTemplateFormatVersion: '2015-09-01'
Description: Compliant - Security group ingress with specific port range

Resources:
  # Compliant: Security group with specific port
  SecurityGroup:
    Type: ALIYUN::ECS::SecurityGroup
    Properties:
      SecurityGroupName: test-sg
      SecurityGroupIngress:
        - IpProtocol: tcp
          PortRange: 443/443
          SourceCidrIp: 0.0.0.0/0

