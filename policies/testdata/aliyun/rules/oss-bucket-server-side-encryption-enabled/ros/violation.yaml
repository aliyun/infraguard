ROSTemplateFormatVersion: '2015-09-01'
Description: Violation - OSS bucket without server-side encryption

Resources:
  OSSBucket:
    Type: ALIYUN::OSS::Bucket
    Properties:
      BucketName: unencrypted-bucket
      AccessControl: private

