ROSTemplateFormatVersion: '2015-09-01'
Description: Compliant - OSS bucket with logging enabled

Resources:
  OSSBucket:
    Type: ALIYUN::OSS::Bucket
    Properties:
      BucketName: data-bucket
      LoggingConfiguration:
        TargetBucket: log-bucket
        TargetPrefix: logs/

