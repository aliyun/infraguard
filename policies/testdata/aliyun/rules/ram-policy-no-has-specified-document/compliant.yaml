ROSTemplateFormatVersion: '2015-09-01'
Resources:
  # Compliant: RAM policy without admin access
  ManagedPolicy:
    Type: ALIYUN::RAM::ManagedPolicy
    Properties:
      PolicyName: restricted-policy
      PolicyDocument:
        Version: '1'
        Statement:
          - Effect: Allow
            Action:
              - 'oss:ListBuckets'
            Resource:
              - '*'

