{
  "version": "2026-03-13",
  "associationProperties": {
    "ALIYUN::ACR::Namespace::Name": {
      "description": "Queries ACR image namespaces.",
      "category": "ACR",
      "metadata": {
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "Status": {
          "description": "The namespace status. Values: NORMAL, DELETING.",
          "valueType": "String"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::ACR::Repo::RepoAttribute": {
      "description": "Queries ACR personal edition image repositories.",
      "category": "ACR",
      "metadata": {
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::ACR::Repo::Tag": {
      "description": "Queries ACR image tag information.",
      "category": "ACR",
      "metadata": {
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "RepoName": {
          "description": "The image repository name.",
          "valueType": "${Parameter}"
        },
        "RepoNamespace": {
          "description": "The namespace name.",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::ALB::ACL::ACLId": {
      "description": "ALB access control list.",
      "category": "ALB",
      "metadata": {
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::ALB::Instance::InstanceId": {
      "description": "ALB instance.",
      "category": "ALB",
      "metadata": {
        "AddressType": {
          "description": "The network type.",
          "valueType": "String"
        },
        "PayType": {
          "description": "The billing method of the ALB instance.",
          "valueType": "String"
        },
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        },
        "VpcId": {
          "description": "The VPC ID.",
          "valueType": "${Parameter}"
        },
        "ZoneId": {
          "description": "The zone ID.",
          "valueType": "${Parameter}"
        }
      }
    },
    "ALIYUN::ALB::LoadBalancer::LoadBalancerId": {
      "description": "ALB load balancer ID.",
      "category": "ALB",
      "metadata": {
        "AddressType": {
          "description": "The network type.",
          "valueType": "String"
        },
        "PayType": {
          "description": "The billing method of the ALB instance.",
          "valueType": "String"
        },
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        },
        "VpcId": {
          "description": "The VPC ID.",
          "valueType": "${Parameter}"
        },
        "ZoneId": {
          "description": "The zone ID.",
          "valueType": "${Parameter}"
        }
      }
    },
    "ALIYUN::APIG::Gateway::GatewayId": {
      "description": "Queries cloud-native API gateway information.",
      "category": "APIG",
      "metadata": {
        "Keyword": {
          "description": "Search keyword, case-insensitive exact match.",
          "valueType": "String"
        },
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::BSS::PricingModule::ModuleCode": {
      "description": "Queries the pricing module information of an Alibaba Cloud product.",
      "category": "BSS",
      "metadata": {
        "ProductCode": {
          "description": "The product code.",
          "valueType": "${Parameter}"
        },
        "ProductType": {
          "description": "The product type.",
          "valueType": "${Parameter}"
        },
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "SubscriptionType": {
          "description": "The subscription type. Values: Subscription (prepaid), PayAsYouGo (postpaid).",
          "valueType": "String"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::Bailian::ApiKey::ApiKeyInfo": {
      "description": "Queries Alibaba Cloud Bailian API-KEY information.",
      "category": "Bailian",
      "metadata": {
        "OnlyKey": {
          "description": "Whether to return only the API key itself.",
          "valueType": "Boolean"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::CAS::Certificate::CertificateId": {
      "description": "SLB instance certificate.",
      "category": "CAS",
      "metadata": {
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::CEN::Instance::CenId": {
      "description": "Queries CEN (Cloud Enterprise Network) instance information under the current account.",
      "category": "CEN",
      "metadata": {
        "ResourceGroupId": {
          "description": "The resource group ID of the CEN instance.",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::CEN::TransitRouter::TransitRouterId": {
      "description": "Queries transit router instance information under a CEN instance.",
      "category": "CEN",
      "metadata": {
        "CenId": {
          "description": "The CEN instance ID.",
          "valueType": "${Parameter}"
        },
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::CMS::Alarm::MetricSelector": {
      "description": "Queries CloudMonitor metric details.",
      "category": "CMS",
      "metadata": {
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::CMS::Alarm::Rule": {
      "description": "Threshold alarm rule.",
      "category": "CMS",
      "metadata": {
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::CMS::Alarm::SilenceTime": {
      "description": "Alarm silence period.",
      "category": "CMS",
      "metadata": {
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::CMS::Alarm::TriggerResources": {
      "description": "Alarm resource trigger.",
      "category": "CMS",
      "metadata": {
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::CMS::Event::EventFilterRule": {
      "description": "Event alarm rule.",
      "category": "CMS",
      "metadata": {
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::CMS::Event::EventLevel": {
      "description": "Event alarm level.",
      "category": "CMS",
      "metadata": {
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::CMS::Event::EventName": {
      "description": "Event alarm name.",
      "category": "CMS",
      "metadata": {
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::CMS::Product::ProductType": {
      "description": "Cloud services that support events.",
      "category": "CMS",
      "metadata": {
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::CR::Instance::InstanceId": {
      "description": "Queries Container Registry Enterprise Edition instance information.",
      "category": "CR",
      "metadata": {
        "InstanceStatus": {
          "description": "The instance status. Values: PENDING, INIT_ERROR, STARTING, RUNNING, STOPPING, STOPPED, DELETING, DELETED.",
          "valueType": "String"
        },
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::CR::NameSpace::Name": {
      "description": "Queries Container Registry Enterprise Edition namespace information.",
      "category": "CR",
      "metadata": {
        "InstanceId": {
          "description": "The instance ID.",
          "valueType": "${Parameter}"
        },
        "NamespaceStatus": {
          "description": "The namespace status. Values: NORMAL, DELETING.",
          "valueType": "String"
        },
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::CR::Repository::RepoName": {
      "description": "Queries Container Registry Enterprise Edition repository information.",
      "category": "CR",
      "metadata": {
        "InstanceId": {
          "description": "The instance ID.",
          "valueType": "${Parameter}"
        },
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "RepoStatus": {
          "description": "The repository status. Values: NORMAL, DELETING, DELETED, ALL.",
          "valueType": "String"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::CR::Repository::Tag": {
      "description": "Queries Container Registry Enterprise Edition image tag information.",
      "category": "CR",
      "metadata": {
        "InstanceId": {
          "description": "The instance ID.",
          "valueType": "${Parameter}"
        },
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "RepoId": {
          "description": "The repository ID.",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::CS::Cluster::ClusterId": {
      "description": "Container Service cluster ID.",
      "category": "CS",
      "metadata": {
        "ClusterState": {
          "description": "The cluster state. Values: running, initial, unavailable, all.",
          "valueType": "String"
        },
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::CS::Cluster::ClusterNodePool": {
      "description": "Node pool within a Container Service cluster.",
      "category": "CS",
      "metadata": {
        "ClusterId": {
          "description": "The Container Service cluster ID.",
          "valueType": "${Parameter}"
        },
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::CS::Cluster::KubernetesVersion": {
      "description": "Queries Kubernetes version information.",
      "category": "CS",
      "metadata": {
        "AutoSelectLastest": {
          "description": "Whether to automatically select the latest version.",
          "valueType": "Boolean"
        },
        "ClusterType": {
          "description": "The cluster type. Values: Kubernetes, ManagedKubernetes, ExternalKubernetes.",
          "valueType": "String"
        },
        "KubernetesVersionPrefix": {
          "description": "The Kubernetes version prefix.",
          "valueType": "String"
        },
        "Profile": {
          "description": "The cluster profile. Values: Default, Edge, Serverless.",
          "valueType": "String"
        },
        "Runtime": {
          "description": "The runtime type. Values: docker, containerd, Sandboxed-Container.runv.",
          "valueType": "String"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::CS::ManagedKubernetesCluster::ServiceCidr": {
      "description": "Queries the Service CIDR of a managed Kubernetes cluster.",
      "category": "CS",
      "metadata": {
        "ClusterType": {
          "description": "The cluster type.",
          "valueType": "String"
        },
        "ContainerCidr": {
          "description": "The container CIDR block.",
          "valueType": "${Parameter}"
        },
        "NodeCidrMask": {
          "description": "The maximum number of IP addresses per node.",
          "valueType": "Integer"
        },
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        },
        "VpcId": {
          "description": "The VPC ID.",
          "valueType": "${Parameter}"
        }
      }
    },
    "ALIYUN::ComputeNest::Artifact::ArtifactId": {
      "description": "Queries ComputeNest artifact information.",
      "category": "ComputeNest",
      "metadata": {
        "ArtifactType": {
          "description": "The artifact type.",
          "valueType": "String"
        },
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::ComputeNest::Artifact::ArtifactIdVersion": {
      "description": "Queries ComputeNest artifact version information.",
      "category": "ComputeNest",
      "metadata": {
        "ArtifactId": {
          "description": "The artifact ID.",
          "valueType": "${Parameter}"
        },
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::ComputeNest::Service::ServiceId": {
      "description": "ComputeNest service ID.",
      "category": "ComputeNest",
      "metadata": {
        "InUsed": {
          "description": "Whether the service is in use.",
          "valueType": "Boolean"
        },
        "IsVendor": {
          "description": "Whether the service has a vendor.",
          "valueType": "Boolean"
        },
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::ComputeNest::ServiceInstance::ServiceInstanceId": {
      "description": "ComputeNest service instance ID.",
      "category": "ComputeNest",
      "metadata": {
        "DeployType": {
          "description": "The deployment type.",
          "valueType": "String"
        },
        "IsVendor": {
          "description": "Whether the service has a vendor.",
          "valueType": "Boolean"
        },
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "ServiceId": {
          "description": "The service ID.",
          "valueType": "${Parameter}"
        },
        "ServiceType": {
          "description": "The service type.",
          "valueType": "String"
        },
        "Status": {
          "description": "The service status.",
          "valueType": "String"
        },
        "Version": {
          "description": "The service version.",
          "valueType": "String"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::ComputeNestSupplier::Service::ServiceVersion": {
      "description": "ComputeNest service version.",
      "category": "ComputeNest",
      "metadata": {
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "ServiceId": {
          "description": "The service ID.",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::DNS::Domain::ValidateDomain": {
      "description": "Validates DNS domain availability.",
      "category": "DNS",
      "metadata": {
        "DomainType": {
          "description": "The domain type.",
          "valueType": "String"
        },
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::DashVector::ApiKey": {
      "description": "Queries DashVector API-KEY.",
      "category": "DashVector",
      "metadata": {
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::Domain::DomainName": {
      "description": "Queries domain name information.",
      "category": "Domain",
      "metadata": {
        "CheckICP": {
          "description": "Whether to check ICP filing status.",
          "valueType": "Boolean"
        },
        "MaxLength": {
          "description": "The maximum domain name length.",
          "valueType": "Integer"
        },
        "ShowDomainPrefixInput": {
          "description": "Whether to allow inputting a domain prefix.",
          "valueType": "Boolean"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::EAS::Instance::InstanceType": {
      "description": "Queries EAS instance types.",
      "category": "EAS",
      "metadata": {
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "SubscriptionType": {
          "description": "The billing type. Values: Subscription (prepaid), PayAsYouGo (postpaid).",
          "valueType": "String"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::EAS::Resource::ResourceId": {
      "description": "Queries EAS resource group information.",
      "category": "EAS",
      "metadata": {
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::ECD::Bundle::DesktopType": {
      "description": "Queries cloud desktop specification details.",
      "category": "ECD",
      "metadata": {
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::ECI::ContainerGroup::ContainerGroupId": {
      "description": "Queries ECI container group information.",
      "category": "ECI",
      "metadata": {
        "ComputeCategory": {
          "description": "The compute category.",
          "valueType": "String"
        },
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "SecurityGroupId": {
          "description": "The security group ID of the instance.",
          "valueType": "${Parameter}"
        },
        "Status": {
          "description": "The instance status. Values: Pending, Running, Succeeded, Failed, Scheduling, ScheduleFailed, Restarting, Updating, Terminating, Expired.",
          "valueType": "String"
        },
        "VSwitchId": {
          "description": "The vSwitch ID of the instance.",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        },
        "ZoneId": {
          "description": "The zone ID of the instance.",
          "valueType": "${Parameter}"
        }
      }
    },
    "ALIYUN::ECS::Command::CommandId": {
      "description": "Cloud Assistant command or Alibaba Cloud command ID.",
      "category": "ECS",
      "metadata": {
        "ImageId": {
          "description": "The image ID. Filters commands by the image OS type.",
          "valueType": "${Parameter}"
        },
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::ECS::DeploymentSet::DeploymentSetId": {
      "description": "Queries ECS deployment set IDs.",
      "category": "ECS",
      "metadata": {
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "Strategy": {
          "description": "The deployment strategy. Values: Availability, AvailabilityGroup.",
          "valueType": "String"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::ECS::Disk::DataDiskCategory": {
      "description": "Data disk category.",
      "category": "ECS",
      "metadata": {
        "InstanceType": {
          "description": "The instance type.",
          "valueType": "${Parameter}"
        },
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        },
        "ZoneId": {
          "description": "The zone ID.",
          "valueType": "${Parameter}"
        }
      }
    },
    "ALIYUN::ECS::Disk::DiskId": {
      "description": "Disk ID.",
      "category": "ECS",
      "metadata": {
        "Category": {
          "description": "The disk category.",
          "valueType": "String"
        },
        "DiskChargeType": {
          "description": "The disk billing method.",
          "valueType": "String"
        },
        "DiskType": {
          "description": "The disk type.",
          "valueType": "String"
        },
        "InstanceId": {
          "description": "The instance ID.",
          "valueType": "${Parameter}"
        },
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "SnapshotId": {
          "description": "The snapshot ID.",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        },
        "ZoneId": {
          "description": "The zone ID.",
          "valueType": "${Parameter}"
        }
      }
    },
    "ALIYUN::ECS::Disk::SystemDiskCategory": {
      "description": "System disk category.",
      "category": "ECS",
      "metadata": {
        "InstanceType": {
          "description": "The instance type.",
          "valueType": "${Parameter}"
        },
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        },
        "ZoneId": {
          "description": "The zone ID.",
          "valueType": "${Parameter}"
        }
      }
    },
    "ALIYUN::ECS::Image::ImageId": {
      "description": "Image ID.",
      "category": "ECS",
      "metadata": {
        "Architecture": {
          "description": "The image architecture.",
          "valueType": "String"
        },
        "ImageFamily": {
          "description": "The image family name.",
          "valueType": "String"
        },
        "InstanceType": {
          "description": "The instance type.",
          "valueType": "${Parameter}"
        },
        "IsSupportIoOptimized": {
          "description": "Whether the image supports I/O optimized instances.",
          "valueType": "Boolean"
        },
        "OSType": {
          "description": "The operating system type.",
          "valueType": "String"
        },
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "SupportedImageOwnerAlias": {
          "description": "Supported image owner aliases.",
          "valueType": "List"
        },
        "Usage": {
          "description": "Whether the image is running on an ECS instance.",
          "valueType": "Boolean"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::ECS::Instance::AttributeSelector": {
      "description": "ECS attribute selector.",
      "category": "ECS",
      "metadata": {
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::ECS::Instance::InstanceId": {
      "description": "ECS instance ID.",
      "category": "ECS",
      "metadata": {
        "ChargeType": {
          "description": "The instance billing method.",
          "valueType": "String"
        },
        "DisabledChargeType": {
          "description": "The disabled billing method.",
          "valueType": "Boolean"
        },
        "DisabledInternetChargeType": {
          "description": "The disabled internet billing method.",
          "valueType": "Boolean"
        },
        "DisabledNetworkType": {
          "description": "The disabled network type.",
          "valueType": "Boolean"
        },
        "InstanceType": {
          "description": "The instance type.",
          "valueType": "${Parameter}"
        },
        "InstanceTypeFamily": {
          "description": "The instance type family.",
          "valueType": "${Parameter}"
        },
        "InternetChargeType": {
          "description": "The internet billing method.",
          "valueType": "String"
        },
        "NetworkType": {
          "description": "The network type.",
          "valueType": "String"
        },
        "OSType": {
          "description": "The operating system type.",
          "valueType": "String"
        },
        "Platform": {
          "description": "The operating system platform.",
          "valueType": "String"
        },
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "ShowChargeType": {
          "description": "Whether to show the billing method.",
          "valueType": "Boolean"
        },
        "ShowInternetChargeType": {
          "description": "Whether to show the internet billing method.",
          "valueType": "Boolean"
        },
        "ShowNetworkType": {
          "description": "Whether to show the network type.",
          "valueType": "Boolean"
        },
        "Status": {
          "description": "The instance status.",
          "valueType": "String"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::ECS::Instance::InstancePropertyEditor": {
      "description": "ECS instance property editor.",
      "category": "ECS",
      "metadata": {
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::ECS::Instance::InstanceType": {
      "description": "ECS instance type.",
      "category": "ECS",
      "metadata": {
        "Constraints": {
          "description": "Instance type filter conditions (supports multiple conditions), including InstanceType, InstanceTypeFamily, vCPU, Memory, Architecture, CustomizeFamily, etc.",
          "valueType": "Map"
        },
        "CreateACKClusterParams": {
          "description": "Filters ECS instance types supported by Container Service.",
          "valueType": "Map"
        },
        "InstanceChargeType": {
          "description": "The instance billing method.",
          "valueType": "String"
        },
        "NetworkPlugin": {
          "description": "The network plugin, e.g. terway-eniip.",
          "valueType": "String"
        },
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "SpotStrategy": {
          "description": "The spot pricing strategy for pay-as-you-go instances.",
          "valueType": "String"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        },
        "ZoneId": {
          "description": "The zone ID.",
          "valueType": "${Parameter}"
        }
      }
    },
    "ALIYUN::ECS::Instance::MaxBandwidthIn": {
      "description": "ECS maximum inbound public bandwidth.",
      "category": "ECS",
      "metadata": {
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::ECS::Instance::OSType": {
      "description": "Queries the OS type of an ECS instance.",
      "category": "ECS",
      "metadata": {
        "InstanceId": {
          "description": "The ECS instance ID.",
          "valueType": "${Parameter}"
        },
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::ECS::Instance::Password": {
      "description": "ECS instance password component. Password must be 8-30 characters and contain at least three of: uppercase letters, lowercase letters, digits, and special characters.",
      "category": "ECS",
      "metadata": {
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::ECS::Instance::RenewPeriod": {
      "description": "ECS renewal period.",
      "category": "ECS",
      "metadata": {
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::ECS::Instance::StopMode": {
      "description": "ECS stop mode.",
      "category": "ECS",
      "metadata": {
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::ECS::InstanceType::AvailableInstanceType": {
      "description": "Available ECS instance types.",
      "category": "ECS",
      "metadata": {
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::ECS::KeyPair::KeyPairName": {
      "description": "Key pair.",
      "category": "ECS",
      "metadata": {
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::ECS::LaunchTemplate::LaunchTemplateId": {
      "description": "Instance launch template ID.",
      "category": "ECS",
      "metadata": {
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::ECS::LaunchTemplate::LaunchTemplateVersion": {
      "description": "Instance launch template version.",
      "category": "ECS",
      "metadata": {
        "LaunchTemplateId": {
          "description": "The launch template ID.",
          "valueType": "${Parameter}"
        },
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::ECS::ManagedInstance::InstanceId": {
      "description": "ECS managed instance ID.",
      "category": "ECS",
      "metadata": {
        "OsType": {
          "description": "The OS type of the managed instance.",
          "valueType": "String"
        },
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::ECS::RAM::Role": {
      "description": "RAM role assigned to the ECS instance.",
      "category": "ECS",
      "metadata": {
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::ECS::RegionId": {
      "description": "Region ID (defaults to the region of the stack).",
      "category": "ECS",
      "metadata": {
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::ECS::RegionId::RegionDeploy": {
      "description": "Sets the ECS deployment region.",
      "category": "ECS",
      "metadata": {
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::ECS::RegionId::TargetRegionIds": {
      "description": "ECS instance target region IDs.",
      "category": "ECS",
      "metadata": {
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::ECS::ResourceGroup::ResourceGroupId": {
      "description": "Resource group.",
      "category": "ECS",
      "metadata": {
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::ECS::SecurityGroup::PortRange": {
      "description": "Sets ECS security group port range based on IP protocol.",
      "category": "ECS",
      "metadata": {
        "IpProtocol": {
          "description": "The IP protocol. Values: ALL, TCP, UDP, ICMP, GRE, ICMPv6.",
          "valueType": "String"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::ECS::SecurityGroup::SecurityGroupId": {
      "description": "Security group ID.",
      "category": "ECS",
      "metadata": {
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        },
        "VpcId": {
          "description": "The VPC ID.",
          "valueType": "${Parameter}"
        }
      }
    },
    "ALIYUN::ECS::Snapshot::AutoSnapshotPolicyId": {
      "description": "Auto snapshot policy ID.",
      "category": "ECS",
      "metadata": {
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::ECS::Snapshot::SnapshotId": {
      "description": "ECS disk snapshot ID.",
      "category": "ECS",
      "metadata": {
        "Category": {
          "description": "The snapshot category.",
          "valueType": "String"
        },
        "DiskId": {
          "description": "The disk ID.",
          "valueType": "${Parameter}"
        },
        "InstanceId": {
          "description": "The ECS instance ID.",
          "valueType": "${Parameter}"
        },
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "SnapshotType": {
          "description": "The snapshot creation type.",
          "valueType": "String"
        },
        "Status": {
          "description": "The snapshot status.",
          "valueType": "String"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::ECS::TAG": {
      "description": "Tags bound to ECS instances.",
      "category": "ECS",
      "metadata": {
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "ResourceType": {
          "description": "The resource type.",
          "valueType": "String"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::ECS::VPC::VPCId": {
      "description": "VPC ID.",
      "category": "ECS",
      "metadata": {
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::ECS::VSwitch": {
      "description": "VSwitch object.",
      "category": "ECS",
      "metadata": {
        "InstanceType": {
          "description": "The instance type.",
          "valueType": "${Parameter}"
        },
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        },
        "VpcId": {
          "description": "The VPC ID.",
          "valueType": "${Parameter}"
        },
        "ZoneId": {
          "description": "The zone ID.",
          "valueType": "${Parameter}"
        }
      }
    },
    "ALIYUN::ECS::ZoneId": {
      "description": "Queries ECS availability zone IDs.",
      "category": "ECS",
      "metadata": {
        "InstanceChargeType": {
          "description": "The instance billing method.",
          "valueType": "String"
        },
        "InstanceType": {
          "description": "The instance type.",
          "valueType": "${Parameter}"
        },
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "SystemDiskCategory": {
          "description": "The system disk category.",
          "valueType": "String"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        },
        "WithAvailableResource": {
          "description": "Whether to filter zones with available instance type inventory.",
          "valueType": "Boolean"
        }
      }
    },
    "ALIYUN::EHPC::Cluster::ClusterId": {
      "description": "EHPC cluster selection.",
      "category": "EHPC",
      "metadata": {
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::EHPC::FileSystem::FileSystemId": {
      "description": "File system ID.",
      "category": "EHPC",
      "metadata": {
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::EHPC::FileSystem::MountTargetDomain": {
      "description": "Mount target.",
      "category": "EHPC",
      "metadata": {
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "VSwitchId": {
          "description": "The vSwitch ID.",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        },
        "VolumeId": {
          "description": "The Alibaba Cloud NAS instance ID.",
          "valueType": "${Parameter}"
        },
        "VpcId": {
          "description": "The VPC ID.",
          "valueType": "${Parameter}"
        }
      }
    },
    "ALIYUN::ESS::AutoScalingGroup::AutoScalingGroupId": {
      "description": "ESS auto scaling group ID.",
      "category": "ESS",
      "metadata": {
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::ESS::ECIScalingConfiguration::ContainerName": {
      "description": "Queries ECI-type scaling configuration information.",
      "category": "ESS",
      "metadata": {
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "ScalingConfigurationId": {
          "description": "The scaling configuration ID.",
          "valueType": "${Parameter}"
        },
        "ScalingGroupId": {
          "description": "The scaling group ID.",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::ESS::ECIScalingConfiguration::ScalingConfigurationId": {
      "description": "Queries the scaling configuration ID.",
      "category": "ESS",
      "metadata": {
        "AutoSelectFirst": {
          "description": "Whether to auto-select the first item.",
          "valueType": "Boolean"
        },
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "ScalingGroupId": {
          "description": "The scaling group ID.",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::ESS::ScalingConfiguration::ScalingConfigurationId": {
      "description": "Auto Scaling configuration ID.",
      "category": "ESS",
      "metadata": {
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::Elasticsearch::Instance::InstanceType": {
      "description": "Elasticsearch instance type.",
      "category": "Elasticsearch",
      "metadata": {
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::Emr::ECSCluster::ClusterId": {
      "description": "Queries EMR cluster list.",
      "category": "Emr",
      "metadata": {
        "ClusterStates": {
          "description": "Cluster state array. Values: STARTING, START_FAILED, BOOTSTRAPPING, RUNNING, TERMINATING, TERMINATED, TERMINATED_WITH_ERRORS, TERMINATE_FAILED.",
          "valueType": "List"
        },
        "ClusterTypes": {
          "description": "Cluster type list. Values: DATALAKE, OLAP, DATAFLOW, DATASERVING, CUSTOM, HADOOP.",
          "valueType": "List"
        },
        "PaymentTypes": {
          "description": "Payment types. Values: Subscription (prepaid), PayAsYouGo (postpaid).",
          "valueType": "List"
        },
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::FC3::Function::FunctionName": {
      "description": "Queries Function Compute 3.0 function information.",
      "category": "FC3",
      "metadata": {
        "Prefix": {
          "description": "The function name prefix.",
          "valueType": "String"
        },
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::FC::Function::FunctionName": {
      "description": "Queries legacy Function Compute function information.",
      "category": "FC",
      "metadata": {
        "Prefix": {
          "description": "The function name prefix.",
          "valueType": "String"
        },
        "Qualifier": {
          "description": "The function service version or alias.",
          "valueType": "String"
        },
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "ServiceName": {
          "description": "The function service name.",
          "valueType": "${Parameter}"
        },
        "StartKey": {
          "description": "Returns results starting from this key (inclusive) in alphabetical order.",
          "valueType": "String"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::FC::Service::ServiceName": {
      "description": "Queries legacy Function Compute service information.",
      "category": "FC",
      "metadata": {
        "Prefix": {
          "description": "The service name prefix.",
          "valueType": "String"
        },
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "StartKey": {
          "description": "Returns results starting from this key (inclusive) in alphabetical order.",
          "valueType": "String"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::Flow::Connection::ConnectionId": {
      "description": "Lists service connections in Alibaba Cloud DevOps Flow.",
      "category": "Flow",
      "metadata": {
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        },
        "organizationId": {
          "description": "The organization ID (enterprise ID).",
          "valueType": "${Parameter}"
        },
        "sericeConnectionType": {
          "description": "The service connection type.",
          "valueType": "String"
        }
      }
    },
    "ALIYUN::Flow::Organization::OrganizationId": {
      "description": "Queries the organization list of the current user.",
      "category": "Flow",
      "metadata": {
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        },
        "accessLevel": {
          "description": "The user access level.",
          "valueType": "String"
        },
        "minAccessLevel": {
          "description": "The minimum access level for querying organizations with access level greater than or equal to this value.",
          "valueType": "String"
        }
      }
    },
    "ALIYUN::Hologres::Instance::InstanceId": {
      "description": "Queries Hologres instance ID.",
      "category": "Hologres",
      "metadata": {
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        },
        "cmsInstanceType": {
          "description": "The CloudMonitor instance type. Values: standard, follower, mc-acceleration, warehouse, high-memory.",
          "valueType": "String"
        }
      }
    },
    "ALIYUN::InfluxDB::Instance::InstanceType": {
      "description": "InfluxDB instance type.",
      "category": "InfluxDB",
      "metadata": {
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        },
        "ZoneId": {
          "description": "The zone ID.",
          "valueType": "${Parameter}"
        }
      }
    },
    "ALIYUN::Kafka::Instance::InstanceId": {
      "description": "Queries Kafka instance information in a specified region.",
      "category": "Kafka",
      "metadata": {
        "OrderId": {
          "description": "The order ID.",
          "valueType": "${Parameter}"
        },
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::Lindorm::Instance::InstanceId": {
      "description": "Lists Lindorm instances.",
      "category": "Lindorm",
      "metadata": {
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "ServiceType": {
          "description": "The instance type. Values: lindorm, lindorm_multizone, serverless_lindorm, lindorm_standalone, lts.",
          "valueType": "String"
        },
        "SupportEngine": {
          "description": "The supported data engine type. Values: 1 (search), 2 (time series), 4 (wide column), 8 (file).",
          "valueType": "String"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::MongoDB::Instance::InstanceType": {
      "description": "MongoDB instance type.",
      "category": "MongoDB",
      "metadata": {
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::NAS::FileSystem::FileSystemId": {
      "description": "Queries file system information.",
      "category": "NAS",
      "metadata": {
        "FileSystemType": {
          "description": "The file system type. Values: all (default), standard, extreme, cpfs.",
          "valueType": "String"
        },
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        },
        "VpcId": {
          "description": "The VPC ID.",
          "valueType": "${Parameter}"
        }
      }
    },
    "ALIYUN::NAS::FileSystem::MountTargetDomain": {
      "description": "Queries mount target address.",
      "category": "NAS",
      "metadata": {
        "FileSystemId": {
          "description": "The file system ID.",
          "valueType": "${Parameter}"
        },
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::NLB::LoadBalancer::LoadBalancerId": {
      "description": "Network Load Balancer instance ID.",
      "category": "NLB",
      "metadata": {
        "AddressType": {
          "description": "The network type.",
          "valueType": "String"
        },
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        },
        "VpcIds": {
          "description": "The VPC ID list.",
          "valueType": "List"
        },
        "ZoneId": {
          "description": "The zone ID.",
          "valueType": "${Parameter}"
        }
      }
    },
    "ALIYUN::NLB::Zone::ZoneId": {
      "description": "Network Load Balancer availability zone.",
      "category": "NLB",
      "metadata": {
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "ShowRandom": {
          "description": "Whether to show the random allocation option.",
          "valueType": "Boolean"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::OOS::Command::CommandContent": {
      "description": "OOS execution script.",
      "category": "OOS",
      "metadata": {
        "CommandType": {
          "description": "The command type. Values: RunBatScript, RunPowerShellScript, RunShellScript, RunPythonScript, RunPerlScript.",
          "valueType": "String"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::OOS::Component::TimerTrigger": {
      "description": "OOS timer trigger component.",
      "category": "OOS",
      "metadata": {
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::OOS::File::FileUrl": {
      "description": "Uploaded file URL.",
      "category": "OOS",
      "metadata": {
        "FileType": {
          "description": "The export format. Values: local, https, github, oss.",
          "valueType": "String"
        },
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::OOS::Package::PackageName": {
      "description": "OOS software package name.",
      "category": "OOS",
      "metadata": {
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::OOS::Package::PackageVersion": {
      "description": "OOS software package version.",
      "category": "OOS",
      "metadata": {
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "TemplateName": {
          "description": "The template name.",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::OOS::Parameter::Value": {
      "description": "OOS common parameter.",
      "category": "OOS",
      "metadata": {
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::OOS::PatchBaseline::PatchBaselineName": {
      "description": "Queries OOS patch baseline information.",
      "category": "OOS",
      "metadata": {
        "Name": {
          "description": "The patch baseline name.",
          "valueType": "String"
        },
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::OOS::SecretParameter::Value": {
      "description": "OOS encrypted parameter.",
      "category": "OOS",
      "metadata": {
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::OOS::Template::Content": {
      "description": "OOS template content.",
      "category": "OOS",
      "metadata": {
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "TemplateName": {
          "description": "The OOS template name.",
          "valueType": "${Parameter}"
        },
        "TemplateReadOnly": {
          "description": "Whether the template is read-only.",
          "valueType": "Boolean"
        },
        "TemplateVersion": {
          "description": "The OOS template version.",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::OOS::Template::TemplateName": {
      "description": "OOS template name.",
      "category": "OOS",
      "metadata": {
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::OOS::Template::TemplateVersion": {
      "description": "OOS template version.",
      "category": "OOS",
      "metadata": {
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "TemplateName": {
          "description": "The template name.",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::OSS::Bucket::BucketName": {
      "description": "OSS bucket name.",
      "category": "OSS",
      "metadata": {
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::OSS::Bucket::Object": {
      "description": "OSS storage object.",
      "category": "OSS",
      "metadata": {
        "ObjectType": {
          "description": "The object storage class.",
          "valueType": "String"
        },
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "ValueType": {
          "description": "The object value type.",
          "valueType": "String"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::OSS::Object::ObjectName": {
      "description": "OSS object name.",
      "category": "OSS",
      "metadata": {
        "BucketName": {
          "description": "The bucket name.",
          "valueType": "${Parameter}"
        },
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "ValueType": {
          "description": "The return address type. Default: OSSUrl.",
          "valueType": "String"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::POLARDB::DBCluster::DBClusterId": {
      "description": "PolarDB instance ID.",
      "category": "PolarDB",
      "metadata": {
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::POLARDB::DBCluster::DBNodeClass": {
      "description": "Queries PolarDB cluster available resources.",
      "category": "PolarDB",
      "metadata": {
        "DBType": {
          "description": "The database engine type. Values: MySQL, PostgreSQL, Oracle.",
          "valueType": "String"
        },
        "DBVersion": {
          "description": "The database engine version.",
          "valueType": "String"
        },
        "PayType": {
          "description": "The billing method. Values: Postpaid (pay-as-you-go), Prepaid (subscription).",
          "valueType": "String"
        },
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        },
        "ZoneId": {
          "description": "The zone ID.",
          "valueType": "${Parameter}"
        }
      }
    },
    "ALIYUN::RAM::Role": {
      "description": "RAM role.",
      "category": "RAM",
      "metadata": {
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::RAM::User": {
      "description": "RAM user.",
      "category": "RAM",
      "metadata": {
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::RDS::Engine::EngineId": {
      "description": "RDS database engine type.",
      "category": "RDS",
      "metadata": {
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::RDS::Engine::EngineVersion": {
      "description": "RDS database engine version.",
      "category": "RDS",
      "metadata": {
        "Engine": {
          "description": "The database engine type.",
          "valueType": "String"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::RDS::Instance::AccountPassword": {
      "description": "RDS database account password.",
      "category": "RDS",
      "metadata": {
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::RDS::Instance::InstanceId": {
      "description": "RDS instance ID.",
      "category": "RDS",
      "metadata": {
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        },
        "ZoneId": {
          "description": "The zone ID.",
          "valueType": "${Parameter}"
        }
      }
    },
    "ALIYUN::RDS::Instance::InstanceType": {
      "description": "RDS instance type.",
      "category": "RDS",
      "metadata": {
        "Category": {
          "description": "The instance series.",
          "valueType": "String"
        },
        "DBInstanceClass": {
          "description": "The instance class.",
          "valueType": "String"
        },
        "DBInstanceStorageType": {
          "description": "The instance storage type.",
          "valueType": "String"
        },
        "DispenseMode": {
          "description": "The allocation mode.",
          "valueType": "String"
        },
        "Engine": {
          "description": "The database engine type.",
          "valueType": "String"
        },
        "EngineVersion": {
          "description": "The database engine version.",
          "valueType": "String"
        },
        "InstanceChargeType": {
          "description": "The instance billing method.",
          "valueType": "String"
        },
        "OrderType": {
          "description": "The order type.",
          "valueType": "String"
        },
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        },
        "ZoneId": {
          "description": "The zone ID.",
          "valueType": "${Parameter}"
        }
      }
    },
    "ALIYUN::Redis::Instance::InstanceId": {
      "description": "Redis instance ID.",
      "category": "Redis",
      "metadata": {
        "ChargeType": {
          "description": "The billing method.",
          "valueType": "String"
        },
        "EditionType": {
          "description": "The edition type.",
          "valueType": "String"
        },
        "InstanceClass": {
          "description": "The instance class.",
          "valueType": "String"
        },
        "InstanceStatus": {
          "description": "The instance status.",
          "valueType": "String"
        },
        "InstanceType": {
          "description": "The instance type.",
          "valueType": "${Parameter}"
        },
        "NetworkType": {
          "description": "The network type.",
          "valueType": "String"
        },
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::Redis::Instance::InstanceType": {
      "description": "Redis instance type.",
      "category": "Redis",
      "metadata": {
        "Engine": {
          "description": "The database engine type.",
          "valueType": "String"
        },
        "InstanceChargeType": {
          "description": "The instance billing method.",
          "valueType": "String"
        },
        "OrderType": {
          "description": "The order type.",
          "valueType": "String"
        },
        "ProductType": {
          "description": "The product type.",
          "valueType": "${Parameter}"
        },
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        },
        "ZoneId": {
          "description": "The zone ID.",
          "valueType": "${Parameter}"
        }
      }
    },
    "ALIYUN::Resource::Data::API": {
      "description": "Queries data from Alibaba Cloud product OpenAPI based on parameters.",
      "category": "Custom",
      "metadata": {
        "APIAction": {
          "description": "The cloud product and API information.",
          "valueType": "Map"
        },
        "LabelGetter": {
          "description": "Defines the attribute displayed in the query results.",
          "valueType": "Map"
        },
        "OptionsGetter": {
          "description": "Defines the data hierarchy parsing mode.",
          "valueType": "Map"
        },
        "PageFilterKeys": {
          "description": "Pagination filter attributes.",
          "valueType": "List"
        },
        "PageNumber": {
          "description": "The page number.",
          "valueType": "Integer"
        },
        "PageSize": {
          "description": "The page size for pagination.",
          "valueType": "Integer"
        },
        "Pagination": {
          "description": "The pagination method.",
          "valueType": "Map"
        },
        "TotalCount": {
          "description": "The total count of records.",
          "valueType": "Integer"
        },
        "ValueGetter": {
          "description": "Defines the name of the attribute to parse.",
          "valueType": "Map"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::ResourceManager::Account": {
      "description": "Resource Manager account.",
      "category": "ResourceManager",
      "metadata": {
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::ResourceManager::Folder": {
      "description": "Resource Manager folder.",
      "category": "ResourceManager",
      "metadata": {
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::SAE::Namespace::NamespaceId": {
      "description": "SAE namespace.",
      "category": "SAE",
      "metadata": {
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::SLB::ACL::ACLId": {
      "description": "SLB access control list ID.",
      "category": "SLB",
      "metadata": {
        "AddressIPVersion": {
          "description": "The IP address version.",
          "valueType": "String"
        },
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::SLB::Instance::InstanceType": {
      "description": "SLB instance type.",
      "category": "SLB",
      "metadata": {
        "InstanceChargeType": {
          "description": "The instance billing method.",
          "valueType": "String"
        },
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "SpotStrategy": {
          "description": "The spot pricing strategy for pay-as-you-go instances.",
          "valueType": "String"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        },
        "ZoneId": {
          "description": "The zone ID.",
          "valueType": "${Parameter}"
        }
      }
    },
    "ALIYUN::SLB::LoadBalancer::LoadBalancerId": {
      "description": "SLB instance ID.",
      "category": "SLB",
      "metadata": {
        "AddressType": {
          "description": "The address type.",
          "valueType": "String"
        },
        "InstanceChargeType": {
          "description": "The instance billing method.",
          "valueType": "String"
        },
        "MasterZoneId": {
          "description": "The primary zone ID of the SLB instance.",
          "valueType": "${Parameter}"
        },
        "NetworkType": {
          "description": "The network type.",
          "valueType": "String"
        },
        "PayType": {
          "description": "The billing method of the SLB instance.",
          "valueType": "String"
        },
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "SlaveZoneId": {
          "description": "The secondary zone ID of the SLB instance.",
          "valueType": "${Parameter}"
        },
        "VSwitchId": {
          "description": "The vSwitch ID.",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        },
        "VpcId": {
          "description": "The VPC ID.",
          "valueType": "${Parameter}"
        }
      }
    },
    "ALIYUN::SLB::LoadBalancer::ZoneId": {
      "description": "Queries SLB availability zone information.",
      "category": "SLB",
      "metadata": {
        "MasterZoneId": {
          "description": "The primary zone ID.",
          "valueType": "${Parameter}"
        },
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        },
        "ZoneType": {
          "description": "The zone type. Values: Master (primary, default), Slave (secondary).",
          "valueType": "String"
        }
      }
    },
    "ALIYUN::ServiceCatalog::LaunchOption::PortfolioId": {
      "description": "Service Catalog portfolio ID.",
      "category": "ServiceCatalog",
      "metadata": {
        "ProductId": {
          "description": "The product ID.",
          "valueType": "${Parameter}"
        },
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::ServiceCatalog::ProductVersion::ProductVersionId": {
      "description": "Product version ID.",
      "category": "ServiceCatalog",
      "metadata": {
        "Active": {
          "description": "Whether the version is active.",
          "valueType": "Boolean"
        },
        "ProductId": {
          "description": "The product ID.",
          "valueType": "${Parameter}"
        },
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::Tag::TagKey": {
      "description": "Queries tag key information.",
      "category": "Tag",
      "metadata": {
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "ResourceType": {
          "description": "The resource type.",
          "valueType": "String"
        },
        "ShowSystem": {
          "description": "Whether to filter system tags.",
          "valueType": "Boolean"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::Tag::TagValue": {
      "description": "Queries tag values for a specified tag key.",
      "category": "Tag",
      "metadata": {
        "Key": {
          "description": "The tag key.",
          "valueType": "${Parameter}"
        },
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "ResourceType": {
          "description": "The resource type.",
          "valueType": "String"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::User::Account::AccountIds": {
      "description": "User account IDs.",
      "category": "User",
      "metadata": {
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::User::AccountId": {
      "description": "Gets the Alibaba Cloud primary account ID.",
      "category": "User",
      "metadata": {
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::User::UserId": {
      "description": "Gets the current Alibaba Cloud user ID.",
      "category": "User",
      "metadata": {
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::VPC::EIP::AllocationId": {
      "description": "Queries Elastic IP address information.",
      "category": "VPC",
      "metadata": {
        "EIPStatus": {
          "description": "The EIP status. Values: Associating, Unassociating, InUse, Available, Releasing.",
          "valueType": "String"
        },
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::VPC::NatGateway::NatGatewayId": {
      "description": "NAT Gateway ID.",
      "category": "VPC",
      "metadata": {
        "InstanceChargeType": {
          "description": "The instance billing method.",
          "valueType": "String"
        },
        "NatType": {
          "description": "The NAT gateway type.",
          "valueType": "String"
        },
        "NetworkType": {
          "description": "The NAT gateway network type.",
          "valueType": "String"
        },
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        },
        "VpcId": {
          "description": "The VPC ID.",
          "valueType": "${Parameter}"
        },
        "ZoneId": {
          "description": "The zone ID.",
          "valueType": "${Parameter}"
        }
      }
    },
    "ALIYUN::VPC::VPC::CidrBlock": {
      "description": "CIDR block for a new VPC.",
      "category": "VPC",
      "metadata": {
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::VPC::VSwitch::CidrBlock": {
      "description": "CIDR block for a new vSwitch.",
      "category": "VPC",
      "metadata": {
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        },
        "VpcCidrBlock": {
          "description": "The VPC CIDR block.",
          "valueType": "${Parameter}"
        },
        "VpcId": {
          "description": "The VPC ID.",
          "valueType": "${Parameter}"
        }
      }
    },
    "ALIYUN::VPC::VSwitch::VSwitchId": {
      "description": "VSwitch ID.",
      "category": "VPC",
      "metadata": {
        "InstanceType": {
          "description": "The instance type.",
          "valueType": "${Parameter}"
        },
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        },
        "VpcId": {
          "description": "The VPC ID.",
          "valueType": "${Parameter}"
        },
        "ZoneId": {
          "description": "The zone ID.",
          "valueType": "${Parameter}"
        }
      }
    },
    "ALIYUN::VPC::VirtualBorderRouter::RouteTableId": {
      "description": "Virtual Border Router ID.",
      "category": "VPC",
      "metadata": {
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ALIYUN::VPC::Zone::ZoneId": {
      "description": "Queries VPC availability zone information.",
      "category": "VPC",
      "metadata": {
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "Auto": {
      "description": "ROS automatically generates AssociationProperty and AssociationPropertyMetadata fields based on the Terraform original data structure. Only applies to JSON-type parameters in Terraform templates.",
      "category": "Basic",
      "metadata": {
        "Overwrite": {
          "description": "Supplements or overwrites nested parameter definitions. Dictionary type.",
          "valueType": "Map"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "AutoCompleteInput": {
      "description": "Automatically generates a random string.",
      "category": "Basic",
      "metadata": {
        "CharacterClasses": {
          "description": "List of character types, each containing Class (lowercase/uppercase/number/specialCharacter), Min, etc.",
          "valueType": "List"
        },
        "Length": {
          "description": "The string length.",
          "valueType": "Integer"
        },
        "Prefix": {
          "description": "The string prefix.",
          "valueType": "String"
        },
        "Suffix": {
          "description": "The string suffix.",
          "valueType": "String"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "ChargeType": {
      "description": "The billing method. Values: PayAsYouGo, Subscription.",
      "category": "Basic",
      "metadata": {
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "Code": {
      "description": "Code input.",
      "category": "Basic",
      "metadata": {
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "CommaDelimitedList": {
      "description": "Input a comma-delimited array.",
      "category": "Basic",
      "metadata": {
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "Cron": {
      "description": "A cron expression string that specifies the execution time of a scheduled task.",
      "category": "Basic",
      "metadata": {
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "DateTime": {
      "description": "Date selection.",
      "category": "Basic",
      "metadata": {
        "GMTZone": {
          "description": "The GMT zone.",
          "valueType": "String"
        },
        "TimeZone": {
          "description": "The time zone description.",
          "valueType": "String"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "FileContent": {
      "description": "Reads content from a local file.",
      "category": "Basic",
      "metadata": {
        "AcceptFileSuffixes": {
          "description": "Accepted file types for upload. Multiple types are separated by commas.",
          "valueType": "String"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "GMTZone": {
      "description": "GMT zone selection.",
      "category": "Basic",
      "metadata": {
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "Json": {
      "description": "JSON object input.",
      "category": "Basic",
      "metadata": {
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "List[Parameter]": {
      "description": "Parameter list with indented layout. Only applies to Json-type parameters.",
      "category": "Basic",
      "metadata": {
        "Mode": {
          "description": "Display style. Collapse: collapsed style (default). Table: table style.",
          "valueType": "String"
        },
        "Parameter": {
          "description": "The parameter definition.",
          "valueType": "Map"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "List[Parameters]": {
      "description": "Parameter group list with table layout. Only applies to Json-type parameters.",
      "category": "Basic",
      "metadata": {
        "ListMetadata": {
          "description": "Dictionary type for controlling table display, including ShowHeader, ShowRemove, ShowAddition, Order, etc.",
          "valueType": "Map"
        },
        "Parameters": {
          "description": "Dictionary type, required. Represents a group of parameter definitions. Key: parameter name. Value: parameter definition.",
          "valueType": "Map"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "OOSServiceRole": {
      "description": "The OOS service role.",
      "category": "Basic",
      "metadata": {
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "Password": {
      "description": "Password input.",
      "category": "Basic",
      "metadata": {
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "RateControl": {
      "description": "The OOS execution rate.",
      "category": "Basic",
      "metadata": {
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "Tags": {
      "description": "Tags.",
      "category": "Basic",
      "metadata": {
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "ResourceType": {
          "description": "The resource type for tag statistics. Default: ALIYUN::ECS::INSTANCE.",
          "valueType": "String"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "Targets": {
      "description": "ECS instance targets. Supports selecting instances by instance ID, tags, or resource groups.",
      "category": "Basic",
      "metadata": {
        "RegionId": {
          "description": "The region ID (defaults to the region of the stack).",
          "valueType": "${Parameter}"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "TextArea": {
      "description": "Rich text input.",
      "category": "Basic",
      "metadata": {
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "TimeTriggerWeekly": {
      "description": "Weekly time selection.",
      "category": "Basic",
      "metadata": {
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "TimeZone": {
      "description": "Time zone selection.",
      "category": "Basic",
      "metadata": {
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    },
    "UploadFilesToUserBucket": {
      "description": "OSS file upload component.",
      "category": "OSS",
      "metadata": {
        "AddSuffix": {
          "description": "Whether to append a timestamp suffix to the file name. Value: true.",
          "valueType": "Boolean"
        },
        "BucketName": {
          "description": "The bucket name.",
          "valueType": "${Parameter}"
        },
        "Directory": {
          "description": "Whether to support folder upload. Value: true.",
          "valueType": "Boolean"
        },
        "Mode": {
          "description": "The component display mode. Values: normal, dragger (default).",
          "valueType": "String"
        },
        "Multiple": {
          "description": "Whether to upload multiple files at once. Value: true.",
          "valueType": "Boolean"
        },
        "ObjectNamePrefix": {
          "description": "The upload file path prefix.",
          "valueType": "String"
        },
        "RegionId": {
          "description": "The region of the bucket. Defaults to the current environment region.",
          "valueType": "${Parameter}"
        },
        "SuffixFormat": {
          "description": "The timestamp suffix format, e.g. YYYY-MM-DD.",
          "valueType": "String"
        },
        "ValueType": {
          "description": "The return value type. Default: OSS Url.",
          "valueType": "String"
        },
        "Visible": {
          "description": "Controls the display condition of this parameter. Uses condition functions (Fn::Equals, Fn::Not, Fn::And, Fn::Or) with ${ParameterName} references.",
          "valueType": "Map"
        }
      }
    }
  }
}
